import { ITheme } from "./interfaces";

const TILE_SIZE = 16;
const CANVAS_WIDTH = 640;
const CANVAS_HEIGHT = 320;

const THEME: ITheme = {
    pixelSize: 2
}

export {
    TILE_SIZE,
    CANVAS_HEIGHT,
    CANVAS_WIDTH,
    THEME
}